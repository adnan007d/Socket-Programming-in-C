This is a Server Client Program where client sends some data and server response

# Compiling

`make`

`make clean` to clear all generated files

# Usage

First run the server `./server`

Then run the client `./client`
